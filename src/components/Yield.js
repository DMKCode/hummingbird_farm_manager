import React, { Component } from "react";

class Yield extends Component {
  render() {
    return (
      <section className="yield-details">
        <div>Yield Details</div>
      </section>
    );
  }
}

export default Yield;
